# Flow 素材下载助手 1.0.5

适用桌面 Chrome / Edge，Manifest V3，无需付费服务，无需 API Key。

## 1.0.5 场景完整导出与显示筛选

- 新增 `flow-scene-tile` 场景卡片识别，单独显示“场景视频”标签与数量。
- 场景的视频播放器可能包含多个片段地址。这些地址不作为完整场景文件下载；只从已有图片封面生成缩略图。
- 下载场景的流程：定位该场景 → 打开场景编辑页 → 检查当前场景 → 等待顶部“下載場景”按钮可用 → 点击一次 → 捕获本次完整导出的链接 → 保存并核对时间文件夹 → 点击返回，继续原列表下一项。
- 按钮禁用／处理中时最多等待 90 秒，不会重复点击；点击后最多等待 5 分钟获取导出链接。超时会提示未确认完成，不把某个预览片段冒充完整场景。
- 如果无法返回原素材列表，会停止后续任务，避免定位到错误素材。期间保持页面打开，不手动切换场景。
- 显示筛选：全部 / 图片 / 普通视频 / 场景视频。这里控制的是显示类型，与下面的勾选按钮不同。
- 筛选时保留隐藏素材的勾选；全选、框选、Shift 连选、下载只作用于当前显示的类型。要下载所有类型，先切回“全部”。
- 场景文件用场景标题命名，沿用同批时间文件夹和重名编号；普通图片、普通视频沿用原下载逻辑。
- 这是基于提供的场景列表和编辑页 HTML 编写的适配。场景打开、禁用等待、完整导出地址捕获及返回流程已通过模拟测试，尚未在真实 Flow 页面验证场景导出。

## 1.0.4 视频立即识别

- 视频卡片只要有已完成状态或就绪操作控件，即先显示，不再等待播放器的 src 地址。
- 排队、生成中、失败，以及没有任何就绪证据的空视频容器仍不进入可下载列表。
- 立即读取已有封面（poster、视频卡片内的图片或背景图）。没有可读封面时先显示“视频已识别 · 封面待加载”，后续封面出现自动替换，不为识别而强制播放所有视频。
- 播放地址、素材 ID 后续出现时合并到同一卡片，保留勾选，避免再追加一个重复视频。
- 同提示词的多个视频不会只按提示词合并。有稳定 ID 时按 ID 识别；完全没有 ID 时临时按卡片位置和提示词对应。若列表重排后无法可靠定位，会提示找不到素材，不猜测下载另一个同名视频。
- 下载时重新定位视频、补充可读取的地址，再使用“原始大小”菜单获取真正的下载资源。没有预先播放地址也可以进入菜单下载流程。
- 保留 1.0.3 的小面板、滚动缩略图、多选和 0.5～1.5 秒下载间隔。
- 新增 `node tests/lazy-video.cjs`：无 src 视频识别、生成中排除、封面补齐、ID 合并和无预先播放地址的菜单下载模拟。

## 1.0.3 本次更新

- 点击扩展直接打开网页右侧的主面板，去掉中间的小弹窗。正常打开不再需要第二次点击。
- 面板默认最大宽度 540px、高度 700px；小屏幕自动缩小。比旧版 750px 宽、接近满屏的面板更紧凑。
- 缩略图区域独立滚动。卡片固定高度、图片和视频使用完整比例适配，数量多时不压缩卡片、不裁掉缩略图。类型标签放在预览上方。
- 打开时立即显示当前已加载素材；同一项目再次打开保留已有列表和选择。面板打开期间会自动补充新出现的素材。全项目扫描仍可按需点击，不会一打开就滚动网页。
- 每处理一项后、下一项之前随机等待 0.5～1.5 秒；2K 失败回退 1K 前也等待一次。最后一项结束后不额外等待。
- 保留已经由用户确认正常的 1K/2K 图片、原始视频下载流程、提示词文件名和时间文件夹逻辑。

### 选择操作

| 操作 | 效果 |
| --- | --- |
| 点击卡片／复选框 | 勾选或取消当前素材 |
| Ctrl+A（Mac：Command+A） | 全选当前已识别列表 |
| Ctrl+Shift+A | 取消全选 |
| 点击第一项，再 Shift 点击另一项 | 选择两项之间的连续范围 |
| Ctrl+Shift 点击 | 在已有选择上追加连续范围 |
| 按住鼠标左键拖动 | 框选矩形范围内的卡片，替换原选择 |
| Shift 拖动 | 追加框选区域 |
| Ctrl 拖动 | 将框内卡片反选 |
| 拖动到列表上／下边缘 | 自动滚动，可跨屏框选 |
| Escape（框选中） | 取消本次框选，恢复拖动前选择 |
| 箭头键、Home、End | 在卡片之间移动焦点 |
| Shift+箭头 | 从选择起点连续选择到当前卡片 |
| 空格／Enter（卡片聚焦时） | 勾选／取消当前卡片 |
| 工具栏“反选／只选图片／只选视频” | 快速按类型或反向选择 |

快捷键只在下载面板有焦点时生效，避免影响 Flow 提示词输入框。面板打开时自动把焦点放在素材列表。框选和快捷选择在下载中暂时禁用，扫描中仍可用。默认新素材勾选，想从少量素材开始选时可先“取消全选”。

## 1.0.2 本次修复

- 边扫描边显示缩略图与统计，滚动不动时退出并保留结果，不再一直显示空白。
- 扫描中可点击整张卡片、预览、文字勾选或取消；键盘聚焦卡片后按空格/回车也可选择。
- 每项顶部明确显示绿色“图片”或紫色“视频”，顶部汇总分别计数。
- 文件名使用该素材的提示词，不附加开头序号或分辨率后缀；Windows 不允许的字符替换为下划线，长名称截短至 120 字符。重名由浏览器自动添加 (1)、(2) 等编号。
- 如果页面只提供“Volunteers decorating street for…”这类截断提示词，现阶段只能使用可读取的部分；卡片标题提示和扫描结果会说明此限制，不声称取得了完整提示词。
- 捕获链接下载与可匹配到的网页原生下载，都在下载命名阶段指定“时间文件夹/提示词.扩展名”。同一批次使用同一个文件夹。
- 下载完成后检查实际父文件夹。路径不符合预期会显示具体位置，不误报成功，也不自动重下同一文件。
- 自动扫描上限 5 分钟；滚动卡住会更早停止并明确提示扫描不完整。

## 更新方法

停止旧版本任务。覆盖原扩展文件夹后，在扩展管理页点击重新加载；或移除旧扩展、加载新包。**必须刷新 Flow 网页**，旧版页面内脚本才会被替换。主面板标题应显示版本 1.0.5。

## 安装

1. 解压 ZIP，保留整个 `flow-downloader` 文件夹。
2. Chrome 地址栏打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
3. 开启右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”，选择包含 `manifest.json` 的 `flow-downloader` 文件夹。不要选择 ZIP 文件。
5. 回到 Flow 项目，刷新网页。点击浏览器工具栏的扩展图标，选择“Flow 素材下载助手”。可以把它固定到工具栏。

## 使用

1. 打开要下载的 Flow 项目，等待卡片显示。
2. 点击扩展图标，直接打开主面板，立即显示已加载素材。
3. 选择“扫描完整列表”：自动滚动当前素材列表，按素材 ID 去重；“刷新已加载”补充当前已进入网页 HTML 的素材，不清空同一项目已有选择。
4. 面板显示图片、视频数量和预览。默认全选；可点击整张卡片取消单项，或用顶部全选框切换。扫描时实时更新。
5. 图片默认下载 1K。勾选“图片优先 2K”后，若 2K 不可用或明确发生网络/服务器下载失败，会回退到 1K。用户取消下载、磁盘问题和状态不明的超时不会盲目重复下载。
6. 视频自动选择标注“原始大小”的下载项，分辨率可能是 360p、720p、1080p 等，不固定数字，也不选择 GIF。
7. 点击“下载选中素材”。同一批次的所有图片、视频保存到浏览器默认下载目录下的同一个子文件夹，例如 `flow下载2026-09-11_21-35-08`。时间取电脑本地时间，24 小时制。
8. 完成后点击“打开下载位置”。文件名使用提示词；相同文件名自动编号。优先根据 MIME 或页面文件名保留常见图片／视频扩展名。

## 下载期间

- 保持 Flow 页面打开，不刷新、不切换项目、不手动滚动或操作下载菜单。面板可收起，任务仍继续。
- “停止后续任务”让正在处理的任务结束后停止队列，不会取消已开始的浏览器下载。
- 只有浏览器报告下载 complete 才显示“已完成”，开始下载并不算成功。
- 若另一个标签页正在使用扩展下载，请等待它结束。
- 通过浏览器下载 API 发起的下载使用 `saveAs:false`。若 Blob 兼容回退进入网页原生下载，浏览器的“下载前询问每个文件的保存位置”设置可能仍弹出保存窗口；要连续自动下载，请在浏览器下载设置关闭该选项。企业策略或其他下载管理扩展仍可能影响保存行为。

## 实现方式及边界

- 不猜测 2K URL，不把预览图片直接当作 1K/2K 成品。
- 自动打开对应素材的菜单、选择指定质量，捕获该操作生成的真实 HTTPS 或 Blob 下载地址，再交给浏览器下载管理器保存。页面控件会自动展开，但无需手动移动鼠标。
- 捕获只在当前任务的短时间窗口内启用，不记录账号 Cookie、访问令牌或全部网络请求，不向第三方服务器发送素材。
- Blob 地址属于当前网页且是临时资源。下载器会延迟页面对该 Blob 的释放，完成后释放；若扩展直接下载 Blob 失败，会尝试页面原生下载，并仅为匹配到的确切 Blob URL 设置文件夹。
- 若未经过可捕获的链接，但浏览器下载 URL 能对应当前素材资源，也会匹配并指定文件夹；无法准确对应的其他原生下载不会改名，且不会冒充本任务的成功下载。不能保证所有未来页面版本都兼容。
- 扫描只覆盖当前项目当前筛选列表；不展开卡片内折叠的历史版本。自动扫描遇到稳定底部结束，并有上限；网络很慢、无限加载或中途停止时可能只有部分素材，面板会显示相应状态。
- 图片有资源地址且没有等待/失败标记时列入；视频有完成或就绪证据即可列入，不必已有播放地址。扫描时不批量请求文件验证有效性；过期、网络或权限问题会在下载阶段体现。
- 2K 菜单显示可用并不保证成功，也不代表服务器一定已完成提升画质。捕获链接等待 60 秒，单个浏览器下载最多观察 30 分钟；超时后提示检查，不盲目重试，防止重复下载。
- 图片预览使用页面地址；视频优先封面，否则使用静音视频预览首帧。资源过期或网络失败可能导致预览不可见。

## 已完成的验证

- 使用提供的 HTML 检查了 19 张图片、完成/生成中视频的结构以及图片和视频的下载菜单定位。
- Node.js 模拟测试覆盖时间文件夹、文件扩展名、下载状态、跨标签页限制、无关下载不改名、链接捕获、Blob 延迟释放和原生回退路径。
- 所有 JavaScript 通过语法检查。
- 用户已确认 1.0.2 的 1K、2K 图片与视频下载正常。本次保留该下载逻辑；新交互通过本地模拟测试，开发环境仍未完成真实 Flow 页面上的新交互端到端验证。

可选开发测试：安装 Node.js 后，在扩展目录执行 `node tests/verify.cjs`。还可执行 `node tests/open.cjs` 验证入口的错误提示、网站识别和打开确认。另运行 `node tests/scan.cjs`，覆盖真实生产扫描/渲染函数的实时显示、卡片点击、停止和卡住处理。测试包含原生 HTTPS 下载命名与实际目录核对。新增 `node tests/selection.cjs` 覆盖连选、框选、滚动框选、Ctrl+A 和随机下载间隔边界。这些是模拟测试，不会联网下载素材。

## 遇到问题

安装/更新后先刷新 Flow。如果扫描不到素材，确认在项目素材列表中。若提示菜单或链接未找到，把面板的具体错误以及当时展开的下载菜单截图提供给开发者。若显示浏览器下载错误，提供错误码即可，无需提供 Cookie 或账号凭据。

权限：`downloads` 用于下载和完成状态，`storage` 保存本次会话任务归属，`activeTab` 与 `scripting` 用于用户点击扩展后初始化已有的 Flow 标签页。自动页面脚本仅匹配 `labs.google`、`flow.google` 和 `flow.google.com`；用户点击时也可通过实际 Flow 卡片结构识别其他地址，只使用当前标签页的临时授权，不自动扩大到全部网站。

参考：
- https://developer.chrome.com/docs/extensions/reference/api/downloads
- https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts

场景测试：`node tests/scenes.cjs`。
